import { createTheme } from '@mui/material'

export const theme = createTheme({
  pallette: {
    primary: {
      main: '#3d8230',
      light: 'skyblue',
    },
    secondary: {
      main: '#15c630',
    },
    otherColor: {
      main: 'white',
    },
  },
})
